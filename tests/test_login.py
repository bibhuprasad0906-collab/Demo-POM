"""\nParametrized pytest test class for login scenarios.\nDeterministic method names, robust error handling, traceability to story IDs.\n"""\n\nimport pytest\nfrom src.pages.login_page import LoginPage\nfrom src.utils.exceptions import LoginFailedError\n\nclass TestLogin:\n\n    @pytest.mark.story(\"AUTH-001\")\n    def test_AUTH_001_login_valid_web(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-001: Login with valid credentials on Web.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-001\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        try:\n            result = page.login(login_data[\"username\"], login_data[\"password\"])\n            assert result is True, \"Dashboard not loaded\"\n        except LoginFailedError as e:\n            pytest.fail(f\"Login failed: {str(e)}\")\n\n    @pytest.mark.story(\"AUTH-002\")\n    def test_AUTH_002_login_valid_mobile(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-002: Login with valid credentials on Mobile.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-002\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        try:\n            result = page.login(login_data[\"username\"], login_data[\"password\"])\n            assert result is True, \"Dashboard not loaded\"\n        except LoginFailedError as e:\n            pytest.fail(f\"Login failed: {str(e)}\")\n\n    @pytest.mark.story(\"AUTH-003\")\n    def test_AUTH_003_login_invalid_web(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-003: Login with invalid credentials on Web.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-003\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        with pytest.raises(LoginFailedError):\n            page.login(login_data[\"username\"], login_data[\"password\"])\n        assert page.is_visible(page.ERROR_MESSAGE), \"Error message not displayed\"\n\n    @pytest.mark.story(\"AUTH-004\")\n    def test_AUTH_004_login_invalid_mobile(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-004: Login with invalid credentials on Mobile.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-004\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        with pytest.raises(LoginFailedError):\n            page.login(login_data[\"username\"], login_data[\"password\"])\n        assert page.is_visible(page.ERROR_MESSAGE), \"Error message not displayed\"\n\n    @pytest.mark.story(\"AUTH-005\")\n    def test_AUTH_005_account_lockout_web(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-005: Account lockout after repeated failed attempts on Web.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-005\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        # Simulate repeated failed attempts\n        for _ in range(int(login_data[\"attempts\"])):\n            try:\n                page.login(login_data[\"username\"], login_data[\"password\"])\n            except LoginFailedError:\n                pass\n        assert page.is_visible(page.LOCKOUT_MESSAGE), \"Lockout message not displayed\"\n\n    @pytest.mark.story(\"AUTH-006\")\n    def test_AUTH_006_account_lockout_mobile(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-006: Account lockout after repeated failed attempts on Mobile.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-006\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        for _ in range(int(login_data[\"attempts\"])):\n            try:\n                page.login(login_data[\"username\"], login_data[\"password\"])\n            except LoginFailedError:\n                pass\n        assert page.is_visible(page.LOCKOUT_MESSAGE), \"Lockout message not displayed\"\n\n    @pytest.mark.story(\"AUTH-007\")\n    def test_AUTH_007_locked_user_web(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-007: Locked user receives lockout notification on Web.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-007\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        with pytest.raises(LoginFailedError):\n            page.login(login_data[\"username\"], login_data[\"password\"])\n        assert page.is_visible(page.LOCKOUT_MESSAGE), \"Lockout message not displayed\"\n\n    @pytest.mark.story(\"AUTH-008\")\n    def test_AUTH_008_locked_user_mobile(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-008: Locked user receives lockout notification on Mobile.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-008\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        with pytest.raises(LoginFailedError):\n            page.login(login_data[\"username\"], login_data[\"password\"])\n        assert page.is_visible(page.LOCKOUT_MESSAGE), \"Lockout message not displayed\"\n\n    @pytest.mark.story(\"AUTH-009\")\n    def test_AUTH_009_password_toggle_web(self, driver, base_url):\n        \"\"\"\n        Test AUTH-009: Password visibility toggle on Web.\n        \"\"\"\n        page = LoginPage(driver)\n        page.open(base_url)\n        assert page.toggle_password_visibility(), \"Password toggle failed\"\n\n    @pytest.mark.story(\"AUTH-010\")\n    def test_AUTH_010_password_toggle_mobile(self, driver, base_url):\n        \"\"\"\n        Test AUTH-010: Password visibility toggle on Mobile.\n        \"\"\"\n        page = LoginPage(driver)\n        page.open(base_url)\n        assert page.toggle_password_visibility(), \"Password toggle failed\"\n\n    @pytest.mark.story(\"AUTH-011\")\n    def test_AUTH_011_audit_login_attempts(self, driver, base_url, login_data):\n        \"\"\"\n        Test AUTH-011: Audit login attempts for compliance.\n        Note: Actual audit log verification is out of scope for UI automation.\n        \"\"\"\n        if login_data[\"scenario\"] != \"AUTH-011\":\n            pytest.skip(\"Not relevant for this scenario\")\n        page = LoginPage(driver)\n        page.open(base_url)\n        try:\n            page.login(login_data[\"username\"], login_data[\"password\"])\n        except LoginFailedError:\n            pass\n        # Recommend backend audit log validation in integration tests\n\n    @pytest.mark.story(\"AUTH-012\")\n    def test_AUTH_012_accessibility_compliance(self, driver, base_url):\n        \"\"\"\n        Test AUTH-012: Accessibility compliance for login forms.\n        \"\"\"\n        page = LoginPage(driver)\n        page.open(base_url)\n        assert page.is_accessible(), \"Login form not accessible per WCAG 2.1 AA\"\n